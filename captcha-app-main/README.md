🚀 Getting Started
1. Local Development
To run this project on your machine:

Install dependencies:

Launch the server:

View the app:
Navigate to http://127.0.0.1:5000 in your web browser.

2. Deployment (Render.com)
This app is "Cloud-Ready." When deploying to Render:

Build Command: `pip install -r requirements.txt

Start Command: python app.py

Environment: Render automatically assigns the PORT, which the script is configured to detect via os.environ.

🚦 API Reference
🛠️ Technologies Used
Python 3.10+

Flask: Web framework

Flask-CORS: Cross-Origin Resource Sharing for API security

Captcha: Image generation library

JavaScript: Async/Await Fetch API for seamless reloading